# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp

# Dependencies / build output (none today — the game has no build step)
node_modules/
dist/

# Local Claude Code state. launch.json is intentionally tracked: it is the
# dev-server config, not a personal setting.
.claude/settings.local.json
